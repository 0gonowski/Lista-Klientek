package com.example.listaklientek;  // Upewnij się, że klasa jest w odpowiednim pakiecie

import javafx.application.Application;

public class Main {
    public static void main(String[] args) {
        // Wywołanie aplikacji JavaFX (ClientApp)
        Application.launch(ClientApp.class, args);
    }
}
